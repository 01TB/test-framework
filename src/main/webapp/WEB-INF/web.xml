<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- Description de l'application -->
    <display-name>Mon Projet Test du Framework</display-name>

    <!-- Listener pour initialiser le framework au démarrage -->
    <listener>
        <listener-class>servlet.FrameworkInitializer</listener-class>
    </listener>

    <!-- Servlet dispatcher pour gérer les requêtes -->
    <servlet>
        <servlet-name>DispatcherServlet</servlet-name>
        <servlet-class>servlet.DispatcherServlet</servlet-class>
        <multipart-config>
            <file-size-threshold>1048576</file-size-threshold>  <!-- 1MB en bytes -->
            <max-file-size>52428800</max-file-size>             <!-- 50MB -->
            <max-request-size>104857600</max-request-size>      <!-- 100MB -->
        </multipart-config>
    </servlet>

    <!-- Mappage du Servlet -->
    <servlet-mapping>
        <servlet-name>DispatcherServlet</servlet-name>
        <url-pattern>/</url-pattern>  <!-- PAS /* ! -->
    </servlet-mapping>

</web-app>
